<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.whocares.weather.mapper.TestMapper">
	<select id="query" parameterType="String" resultType="org.whocares.weather.client.mapper.Test">
		select * from Test where id = #{id}
	</select>
</mapper>